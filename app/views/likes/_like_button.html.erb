<% if current_user.like_now?(micropost) %>
  <%= form_for(current_user.likes.find_by(micropost_id: micropost.id), html: {method: :delete}) do |f| %>
    <%= hidden_field_tag :micropost_id, micropost.id %>
    <%= f.submit "Not_Like", class: "btn btn-warning btn-xs" %>
  <% end %>
<% else %>
  <%= form_for(current_user.likes.build) do |f| %>
    <%= hidden_field_tag :micropost_id, micropost.id %>
    <%= f.submit "Like!", class: "btn btn-success btn-xs" %>
  <% end %>
<% end %>